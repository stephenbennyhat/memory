#include <iostream>
#include "memory.h"
#include "writefile.h"

void
writemoto(std::ostream& os, memory const& mem, int addrlen, int maxline)
{
    for (memory::const_iterator a = mem.begin(); a != mem.end(); a++)
    {
    }
}
